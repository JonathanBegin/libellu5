<?php

	// URI security
	if (!defined('libellu5'))
		die('Direct access not permitted');

	class project extends group {
		
		/* To construct, use same as group:
		
			__construct(libellu5 $l5, $id = null)
			create (user $you, $name, array $leaders, $info = null, array $members = null)
		
		*/
		
		
	
	}